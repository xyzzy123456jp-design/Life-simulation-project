# 開発進捗ログ (PROGRESS.md)

## 2026-08-11 〜 08-15

- Paytonの顔を変更
- Paytonで口パクできるようになった(瞬きはまだ未対応)
- ジェニファーのキャラ設定: バンクーバーに住んでる、ボーイフレンドはHiro
- 教室、ジェニファーの部屋、映画館、散歩、レストランの場所を追加
- 上記の場所について、ジェニファーに確認しOKだったらそこに移動する仕組みを追加

---

## 2026-08-16

### ストーリー設計
- `docs/STORY.md` を新規作成・3回に分けてGit push
  - キャラクター名Jennifer
  - 出会いのシーン
  - 記憶システム
  - 場所移動の仕組み
  - Jenniferの基本設定(ボーイフレンドHiro・カナダ人・バンクーバー在住)

### 記憶・会話システム実装
- `RealtimeTestActor.h`/`.cpp` に以下を実装
  - ゲーム開始時にJenniferの設定+前回セッションの記憶をRealtime APIのInstructionsへ渡す機能(`BuildJenniferInstructions`)
  - セッション終了時に会話ログを保存する機能(`SaveSessionMemory`)

### Codexからのシーン配置作業の引き継ぎ
- `Config/ConversationScenes.ini` による場所ごとの数値設定(リビルド不要)の仕組みを確認・活用
- **教室**: 完全に何も表示されない不具合 → 背景モデルの向きが逆(180度回転で解決)
- **映画館**: 位置・スケール調整、「背中越しにスクリーンを見る構図」に対応(Tick内の自動追従ロジックを修正)
- **彼女の部屋**: 背景モデルが小物サイズだった問題を修正、180度回転、位置調整
- **教室・映画館**: 「外から箱を覗く」見た目だったのを、Jennifer/カメラをモデルの内側まで移動させて解決
- **教室の顔の影**: 専用ライトの強弱・露出補正では直らず、最終的に背景モデル自体のシャドウキャストを無効化して解決
- **レストラン/彼女の部屋の明るさ調整**: カメラの露出補正(ExposureBias、新規ini項目)を追加して対応
- `JenniferScale`が実際のキャラクターに反映されていなかったバグを修正、教室で1.5倍に設定
- **彼女の部屋**: 座りポーズは今回位置合わせのみ対応(アニメーションはCodex側の作業として保留)

### コスト対策
- Realtime APIが起動時に自動接続していたのを変更、**F9キーで接続/切断をトグル**する方式に変更
- 従来方式(Whisper/Chat/TTS)が起動時に自動起動している点を発見(未対応、要相談)

### キャラクター(グラフィック領域)
- ユーザーからMeshy AI生成の新しい顔モデル(zip)の適用依頼 → 役割分担に基づきCodexへ依頼

---

## 2026-08-17

### キャラクター刷新(名前変更・口パク方式の全面見直し)
- キャラクター名をPaytonからJennifer(内部アセット名Crimson)に変更
- MetaHumanの「From Custom Mesh」でMeshyの顔(Crimson Gaze)をMetaHuman化し、標準のARKit LiveLink/RigLogic経由の口パクが正常に動作するようになった(ユーザー確認: 2026-08-17時点で稼働方式は(a) MetaHuman化された正式なリグ経由のARKit LiveLink)
- 元メッシュが重かった(156万頂点/422MB)ためプレイ開始時に約20秒フリーズする問題があり、約39万頂点/107.7MBの軽量版に差し替えて解決

> **⚠️ 記録の食い違いに関する注記**: 同日付でCodex側が作成した`docs/PROGRESS_2026-08-17.md`には、これと異なる記述がある。そちらでは「UE 5.8のFinal Assemblyで`TextureGraph.dll`がクラッシュするため、完成版MetaHumanへの切り替えは保留中」「現在の口パクはMetaHuman化ではなく、Crimson Gazeメッシュに直接jawOpenモーフを追加する簡易方式」とされている。どちらが実際にプロジェクトで稼働している状態か、今後の作業前に実機で確認することを推奨する。

### 顔の差し替え完了報告(Codex作業)
- 新しい顔をデフォルト表示、下唇の頂点調整で開口時の厚みを約30%縮小、専用の上前歯を追加
- **口パクで歯が動かない不具合の修正**
  - 原因: 歯がStatic Meshで、口の開閉はモーフターゲット(頂点変形)のため歯だけ追従せず静止していた
  - 修正: 口の開き具合(MouthOpenAlpha)に応じて歯の位置を疑似的にずらす処理を追加(要確認)

### OpenAI APIクレジット切れの説明
- Realtime API・従来方式ともに同じAPI残高を共有すること、ChatGPT Plusとは別課金であることを説明
- 料金比較: Realtime API 約$0.05〜$0.15/分、従来方式 約$0.01〜$0.02/分(10分でRealtime約80〜240円 vs 従来方式約16〜32円)

### Chromeリモートデスクトップのキーボード不具合
- 症状: 「?」が「・」になる、Shift+Enterが単なるEnterになる(Chromeリモートデスクトップ経由の時のみ発生)
- Windows側のIME設定は正常と確認、ツール側の既知の制約と判断、切断→再接続を試している段階(未解決)

### 未確認・保留中の項目
- 教室のサイズ調整(1.5倍)・歯の追従修正 → 実機確認待ち
- 従来方式(Whisper/Chat/TTS)の自動起動をF9的な仕組みに変更するか → 未回答
- Chromeリモートデスクトップのキーボード不具合 → 未解決

---

## 2026-08-18(月)

### キャラクター設定
- 名前: ジェニファー
- バンクーバー在住
- 高校生
- Hiroがボーイフレンド
- ジェニファーの顔を変更

### 音声・場所移動
- `F9`キーで音声方式(Realtime API / 従来方式)を切り替え
- ジェニファーが合意したら、以下の場所へ移動する
  - Hiroの部屋
  - 教室
  - 映画館
  - ドライブ
  - ジェニファーの部屋
  - ジョギング
  - レストラン
